Place trusted GPG public keys here on the device (not committed to repo).

Suggested path on device:
- /etc/airplay_wyse/trusted-gpg/<keyid>.asc

Ensure `git` is configured to trust these keys for `git verify-tag`.
