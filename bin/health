#!/usr/bin/env bash
set -euo pipefail

STATE_DIR="/var/lib/airplay_wyse"

if [[ -f "$STATE_DIR/last-health.json" ]]; then
  cat "$STATE_DIR/last-health.json"
else
  echo '{"status":"unknown","reason":"no prior converge"}'
fi
