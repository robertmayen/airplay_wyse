#!/usr/bin/env bash
set -euo pipefail

echo "===== converge diagnostics: status ====="
systemctl status --no-pager converge.service || true
echo "===== end status ====="

echo
echo "===== converge diagnostics: recent logs (last 200) ====="
journalctl -u converge.service -n 200 --no-pager || true
echo "===== end recent logs ====="
