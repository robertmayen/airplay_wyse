fqdn: wyse-dac.audio.home.arpa
airplay_name: "Wyse DAC"
nic: eno1
alsa.serial: ""  # optional, if device provides a unique serial
alsa.device_num: 0
alsa.mixer: Digital
# Placeholder inventory for Wyse DAC device
# Replace the USB identifier with the real VID:PID or serial
# Replace the NIC with the actual interface (Debian often uses predictable names like enp*s*, e.g., enp3s0)

device:
  id: "VID:PID-or-serial-goes-here"
network:
  nic: "eno1"  # placeholder; update to real NIC
